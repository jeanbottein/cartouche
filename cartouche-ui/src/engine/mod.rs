//! Reusable retro UI toolkit (the engine layer). M0 ships the foundation:
//! virtual canvas + scaling, aspect modes, the unified input layer, and the
//! palette. Widgets / on-screen keyboard / tweens land in later milestones.

pub mod canvas;
pub mod input;
pub mod text;
pub mod theme;
